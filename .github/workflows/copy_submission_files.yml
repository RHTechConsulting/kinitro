name: Copy Submission Files on Merge to Main


on:
  push:
    branches:
      - main
    paths:
      - 'src/evaluator/rpc/server.py'
      - 'src/evaluator/agent_interface.py'
      - 'src/evaluator/rpc/agent.capnp'

env:
  TMPL_DIR: agent-template

jobs:
  copy-files:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Run copy_submission_files.py
        run: python copy_submission_files.py

  sync-agent-template:
    needs: copy-files
    runs-on: ubuntu-latest
    steps:
      - name: Checkout agent-template repo
        uses: actions/checkout@v4
        with:
          repository: threetau/kinitro-agent-template
          token: ${{ secrets.MINER_TEMPLATE_PUSH_TOKEN }}
          ref: main
          path: ${{ env.TMPL_DIR }}
      - name: Copy updated files from submission_template
        run: |
          cp submission_template/agent_server.py ${{ env.TMPL_DIR }}/agent_server.py
          cp submission_template/agent_interface.py ${{ env.TMPL_DIR }}/agent_interface.py
          cp submission_template/agent.capnp ${{ env.TMPL_DIR }}/agent.capnp
      - name: Commit and push changes
        run: |
          cd ${{ env.TMPL_DIR }}
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add agent_server.py agent_interface.py agent.capnp
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Update agent template files"
            git push origin main
          fi
